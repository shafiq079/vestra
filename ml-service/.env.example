# Internal credential shared only with the Express backend.
ML_SERVICE_KEY=<random-secret-at-least-32-characters>
# Keep API docs disabled in deployed environments. Set true only for local development if needed.
ML_ENABLE_DOCS=false
