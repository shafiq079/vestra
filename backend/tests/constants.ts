/** Shared test-harness constants (kept dependency-free so any file can import it). */

/** Database the suite connects to; asserted by tests/databaseIsolation.test.ts. */
export const TEST_DATABASE_NAME = 'vestra-test';
